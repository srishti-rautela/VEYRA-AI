# Detection pipeline
ultralytics>=8.0.0
supervision>=0.21.0
opencv-python-headless>=4.8.0   # headless variant — works on Railway (no X11 needed)
numpy>=1.24.0

# API backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
httpx>=0.27.0

# Detection stream server (MJPEG / live view)
flask>=3.0.0
flask-cors>=4.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0


ultralytics==8.3.40
torch
torchvision
opencv-python-headless